extends layout

block variables
  - var title = "Dashboard"

block content
  h2.text-center Dashboard

  .row
    .offset-sm-2.col-sm-8
      .jumbotron.text-center
        | Welcome to your dashboard page, #{user.profile.firstName}.  
        br
        | Your ID is #{user.id}. 
        br
        | You can use this to post to /scraper/#{user.id} to save your logs!